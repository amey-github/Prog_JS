/*	These should be sufficient
If not, watch Beau Carnes JS - Array iteration	*/

var arr = ['a', 'b', 'c'];


// forEach(), map(), filter() -- can be replaced with each other

	arr.forEach(function (item, index){			// NOTE : position of ending bracket of forEach(..{}..);
		
		console.log(item, index);
	});

	console.log();